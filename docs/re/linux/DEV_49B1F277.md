# In-game session presentation: dev 49b1f277

Compared both incoming Windows commits and the final 0620342e..49b1f277
first-parent delta. Only menu_hook.cpp, menu_title_panel.inl and the Windows
panel test change. There is no new engine hook, address, byte pattern,
structure offset, calling convention or object lifetime to derive from the ELF.
No speculative addresses or patches were added; no disassembly or gdb probe
was needed for this presentation-only change.

The Linux renderer is independent: panel_linux.cpp uses SDL input and the
mod-owned CPU layer, with presentation in menu_title_linux.inl. Existing
lobby::Snapshot supplies View.inGame from g_gameUiSeen; the existing CGameUI
update integration is documented in [MENU_GAME.md](MENU_GAME.md). This change
uses that signal without changing its engine contract.

MenuPanelMode selects the redesigned renderer for states 1 and 2 in either
world state. TitleMode remains title-only for full-screen backdrop composition
and input capture: expanding it would cover the running world and swallow game
input outside the panel. Linux's existing scale clamp already fits the viewport;
setup now uses 780 x 540 logical pixels even without a public master URL.

Windows Resync action 87 maps to existing Linux action 84, which opens native
WORLD SYNC state 3. Roster actions 114/115 work in-game; title tabs/browser
110..113 remain unavailable there. In-world save selection, start and leave
controls are absent. The existing native hot-join snapshot path is unchanged.
The merged Windows hostSteam gate for Cross-play is applied to both native
lobby and session views, superseding the previous integration's ungated lobby
checkbox. Setup continues to offer Cross-play before a host connection exists.

Offline tests execute the actual renderer and action handlers for host, client,
setup and recovery, including stale savePicker state, two roster pages, modal
isolation, hostSteam visibility and viewport/control bounds at five scales.
The lab launch of this job's soldier libraries failed before game exec with
`bwrap: setting up uid map: Permission denied`. No GPU, on-screen rendering,
loaded world or live input result was observed. Actor copies were restored.

The fractional-scale disjoint-control test found a one-pixel overlap between
adjacent roster rows at scale 0.8. Row positions now advance by the same scaled
height as their hit boxes, matching the Windows row-height calculation.
